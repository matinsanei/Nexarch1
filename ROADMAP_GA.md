
# 🧬 نقشه راه: گذار به معماری الگوریتم ژنتیک (Genetic Algorithm Roadmap)

این سند نقشه راه فنی برای جایگزینی سیستم فعلی (مبتنی بر AI/LLM) با یک موتور تولید پلان خالص مبتنی بر **الگوریتم ژنتیک (Evolutionary Computation)** را ترسیم می‌کند.
هدف: تولید بهینه‌ترین پلان ممکن با استفاده از ریاضیات محض، بدون دخالت شبکه‌های عصبی، با قابلیت امتیازدهی دقیق و علمی.

---

## فاز ۱: زیرساخت داده‌ای و سیستم گرید (The Grid System)

قبل از هرگونه تکامل، باید دنیای دیجیتالی پلان را تعریف کنیم.

*   [ ] **تعریف کروموزوم (Chromosome Definition):**
    *   تبدیل زمین به یک ماتریس شبکه بندی شده (Grid)، مثلاً ۵۰x۵۰ سانتیمتر.
    *   هر "اتاق" در ژنوم به صورت یک بردار تعریف می‌شود: `{type, startX, startY, width, height}`.
    *   ژنوم کل ساختمان = آرایه‌ای از اتاق‌ها.
*   [ ] **موتور هندسی (Geometry Engine):**
    *   نوشتن توابع برای تشخیص برخورد (Collision Detection) برای جلوگیری از روی هم افتادن اتاق‌ها.
    *   توابع برای تشخیص "اتصال" (Adjacency) جهت بررسی همسایگی اتاق‌ها.

## فاز ۲: تابع برازش (Fitness Function) - قلب تپنده سیستم

این بخش حیاتی‌ترین قسمت است. الگوریتم باید بداند "طرح خوب" چیست. امتیاز نهایی مجموع وزن‌دار پارامترهای زیر است:

*   [ ] **امتیاز همجواری (Adjacency Score):**
    *   آشپزخانه نزدیک پذیرایی (+ امتیاز).
    *   حمام مستر داخل اتاق خواب (+ امتیاز).
    *   سرویس بهداشتی عمومی دور از دید مستقیم پذیرایی (+ امتیاز).
*   [ ] **امتیاز نورگیری (Daylight Factor):**
    *   اتاق خواب و پذیرایی چسبیده به لبه‌های نورگیر زمین (+ امتیاز).
    *   سرویس‌ها و راه پله در نقاط کور یا میانی (+ امتیاز).
*   [ ] **امتیاز ابعاد و تناسبات (Aspect Ratio Score):**
    *   جریمه سنگین برای اتاق‌های خیلی باریک یا خیلی کشیده.
    *   پاداش برای اتاق‌های مربعی یا مستطیل طلایی.
*   [ ] **امتیاز مساحت (Area Compliance):**
    *   انحراف از مساحت استاندارد (مثلاً اتاق ۱۲ متری) = کسر امتیاز.
*   [ ] **امتیاز سازه و داکت (Structural Score):**
    *   هم‌راستایی دیوارها برای ستون‌گذاری راحت‌تر.

## فاز ۳: موتور تکامل (The Evolution Engine)

پیاده‌سازی چرخه زندگی الگوریتم ژنتیک.

*   [ ] **تولید جمعیت اولیه (Initialization):**
    *   تولید ۱۰۰۰ پلان تصادفی (اکثراً نامعتبر، ولی شروع کار).
*   [ ] **انتخاب (Selection):**
    *   استفاده از روش "Tournament Selection" یا "Roulette Wheel" برای انتخاب والدین برتر بر اساس Fitness Function.
*   [ ] **ترکیب (Crossover):**
    *   ترکیب ژن‌های دو پلان والد. مثلاً: نصف اتاق‌ها از پدر، نصف از مادر (با اصلاح تداخل‌ها).
*   [ ] **جهش (Mutation):**
    *   تغییر تصادفی کوچک برای جلوگیری از گیر کردن در "بهینه محلی".
    *   مثال: جابجا کردن یک اتاق به اندازه ۱ متر، تغییر ابعاد اتاق خواب، تعویض جای آشپزخانه و خواب.
*   [ ] **نخبه‌گرایی (Elitism):**
    *   حفظ ۵٪ از بهترین پلان‌های هر نسل بدون تغییر برای نسل بعد.

## فاز ۴: محدودیت‌های سخت (Hard Constraints)

قوانینی که اگر نقض شوند، پلان فوراً حذف می‌شود (امتیاز صفر).

*   [ ] **قانون ۶۰٪ + ۲ متر:** عدم تجاوز از محدوده ساخت مجاز شهرداری.
*   [ ] **دسترسی:** هیچ اتاقی نباید ایزوله باشد (باید مسیر دسترسی به در ورودی داشته باشد).
*   [ ] **حداقل عرض:** حذف فضاهای زیر عرض استاندارد (مثلاً راهرو زیر ۹۰ سانت).

## فاز ۵: بصری‌سازی فرآیند (Evolution Visualization)

نمایش زنده فرآیند تکامل به کاربر برای ایجاد حس "خلق کردن".

*   [ ] نمایش نمودار پیشرفت Fitness در طول نسل‌ها.
*   [ ] نمایش زنده بهترین پلان هر نسل روی صفحه (کاربر می‌بیند که پلان از آشوب به نظم می‌رسد).
*   [ ] امکان توقف تکامل و ویرایش دستی توسط کاربر و ادامه تکامل از آن نقطه (Human-in-the-loop).

## فاز ۶: بهینه‌سازی چند هدفه (Multi-Objective Optimization)

*   [ ] استفاده از **NSGA-II** (Non-dominated Sorting Genetic Algorithm II).
*   [ ] ارائه چند خروجی به کاربر در "جبهه پارتو" (Pareto Front):
    *   گزینه ۱: بیشترین مساحت مفید (حتی اگر کمی زشت باشد).
    *   گزینه ۲: بهترین نورگیری (حتی اگر کمی پرتی داشته باشد).
    *   گزینه ۳: کم‌هزینه‌ترین اجرا (ساده‌ترین فرم).

---
**تخمین زمان پیاده‌سازی:** ۳ ماه کار تیمی مهندسی نرم‌افزار و معماری.
